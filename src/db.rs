use crate::session;

hello
#[derive(Debug)]
pub struct MemDb {
    path: std::string::String
}

impl MemDb {
    pub fn get(&self, key: &str) {
        
    }

    pub fn put(&self, key: &str, value: &str) {
        std::fs:: (path)?;
    }

    pub fn delete(&self) {}
}
